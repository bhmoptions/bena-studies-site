/**
 * Ciência Espacial - jogo.js
 * Matéria: Ciências
 * Modelo: Jogo de Duas Áreas
 */

document.addEventListener('DOMContentLoaded', () => {
  console.log('Inicializando Ciência Espacial...');

  const gameControl = {
    state: 'initial',
    score: 0,
    init() {
      // Espaço reservado para carregar dados, montar listeners e iniciar loop/estado do jogo
    }
  };

  gameControl.init();
});
